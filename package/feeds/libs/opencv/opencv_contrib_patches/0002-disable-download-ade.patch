diff --git a/modules/gapi/cmake/DownloadADE.cmake b/modules/gapi/cmake/DownloadADE.cmake
index 8ddaadb..527f162 100644
--- a/moduels/gapi/cmake/DownloadADE.cmake
+++ b/modules/gapi/cmake/DownloadADE.cmake
@@ -2,20 +2,20 @@ set(ade_src_dir "${OpenCV_BINARY_DIR}/3rdparty/ade")
 set(ade_filename "v0.1.2e.zip")
 set(ade_subdir "ade-0.1.2e")
 set(ade_md5 "962ce79e0b95591f226431f7b5f152cd")
-ocv_download(FILENAME ${ade_filename}
-             HASH ${ade_md5}
-             URL
-               "${OPENCV_ADE_URL}"
-               "$ENV{OPENCV_ADE_URL}"
-               "https://github.com/opencv/ade/archive/"
-             DESTINATION_DIR ${ade_src_dir}
-             ID ADE
-             STATUS res
-             UNPACK RELATIVE_URL)
-
-if (NOT res)
-    return()
-endif()
+#ocv_download(FILENAME ${ade_filename}
+#             HASH ${ade_md5}
+#             URL
+#               "${OPENCV_ADE_URL}"
+#               "$ENV{OPENCV_ADE_URL}"
+#               "https://github.com/opencv/ade/archive/"
+#             DESTINATION_DIR ${ade_src_dir}
+#             ID ADE
+#             STATUS res
+#             UNPACK RELATIVE_URL)
+#
+#if (NOT res)
+#    return()
+#endif()
 
 set(ADE_root "${ade_src_dir}/${ade_subdir}/sources/ade")
 file(GLOB_RECURSE ADE_sources "${ADE_root}/source/*.cpp")
